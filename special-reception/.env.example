# Generate a unique random value in the deployment secret provider.
# Never commit the real value or share it with open-reception.
SESSION_SIGNING_SECRET=replace-with-at-least-32-random-characters

# Railway or another runtime normally injects PORT.
PORT=8080
